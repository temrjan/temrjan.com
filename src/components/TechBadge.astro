---
interface Props {
  name: string;
}

const { name } = Astro.props;
---

<span class="tech-badge inline-block rounded-full border border-border bg-surface px-3 py-1 text-xs font-medium text-text-secondary transition-colors hover:border-primary/50 hover:text-text">
  {name}
</span>
